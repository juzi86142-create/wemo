# Platform 数据

- `Language` 与 `MarketLocale` 将语言、locale 和市场分开建模；逻辑 ID 由本地化服务在事务内校验。
- 市场、系统设置、审计日志和可靠 outbox 是平台首批基础表。
- 设置只保存公开配置或密钥引用，不保存可回传前端的明文密钥。
- 审计只追加；outbox 与业务写入同事务，供单体进程中的异步消费者处理通知、索引和 Webhook。
