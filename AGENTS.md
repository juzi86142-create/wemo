# WEMOVE SPORTS 工程协作说明

## 目标与范围

- 唯一业务基准是仓库根目录的 `网站重构需求.md`。
- `www.wemovetoy.com` 仅作为旧站公开资产盘点和 URL 迁移来源；内容上线前必须由品牌方确认版权、规格和合规性。
- 使用 TypeScript 编写全部前后端代码，前后端分离，React 负责前端，Node.js 负责 API。
- 不新增 JavaScript 源码或脚本；构建工具配置也使用 TypeScript 版本。
- 后端是单一模块化单体，所有领域模块随 `apps/api` 同进程部署，禁止拆分微服务。
- Docker 仅用于本地 PostgreSQL、Redis、MinIO、Mailpit 等中间件；前端和 API 不容器化。
- 以模块化单体作为首期后端形态；除非需求明确证明必要，不提前拆微服务。

## 包与依赖方向

- `apps/storefront` 是唯一前端应用，覆盖公开站、用户中心、经销商中心和管理后台；不得再创建第二个前端应用。
- `apps/*` 可以依赖 `packages/*`。
- `packages/contracts` 不依赖应用或数据库。
- `packages/ui` 可依赖 `contracts`，不得依赖任何应用。
- `packages/database` 可依赖 `contracts`，不得依赖任何应用。
- 前端不得直接依赖 `packages/database`，只能通过 API 契约访问业务数据。

## 强制规则

- 数据库禁止物理外键；逻辑关联字段使用 `{entity}_id`，建立必要索引，由服务层检查存在性和租户/企业边界。
- 普通用户、经销商企业与后台权限必须在服务端校验；隐藏前端按钮不能视为授权。
- 经销商价格、订单、报价、地址和私有文件必须按 `company_id` 隔离。
- 订单、报价、价格、付款和审计数据使用快照或历史记录，不因当前配置变化而改写历史事实。
- 对外服务通过 adapter 接入；密钥只能来自环境或密钥管理，不得进入仓库。
- 需求实现完成时必须更新 `docs/requirements-traceability.md` 的状态和自动化/人工验收证据。

## 完成定义

一个需求只有在代码、必要的数据迁移、自动化测试、权限验证、可观测性和文档同步完成后才能标记为 `done`。P0/P1/P2 仅表示实施顺序，不代表可永久省略；最终验收必须覆盖需求文档全部章节、页面、API、状态机与质量属性。
