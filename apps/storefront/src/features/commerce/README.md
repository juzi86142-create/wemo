# Commerce 电商模块

## 页面任务

- 游客购物车、登录合并、数量调整、移除和结算入口。
- B2C 结算表单、订单成功页、订单快照和预览/不可用状态。
- 价格、库存、税费、运费、支付和订单状态以后端响应与共享 contracts 为准。

## 数据边界

购物车和结算通过 `/api/v1` 请求边界访问；前端不复制最终金额、库存或支付规则。没有真实服务时只显示明确的预览或不可用状态，不伪造真实订单。

## 验收范围

覆盖购物车、结算、订单成功页及 `PLP/PDP` 的购买入口；真实数据库、Redis、支付和订单持久化由 API 集成测试负责。
