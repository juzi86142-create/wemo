import { DealerWorkspace, dealerAddresses, dealerCompany, dealerTeam } from "../../../../features/dealer";

export default function DealerCompanyPage() {
  return <DealerWorkspace title="Your company, in motion." description="Company, team, and delivery details are fixed to this local account context, never selected from a user-provided company ID."><section className="dealer-company-grid"><article className="dealer-workspace-panel"><p className="eyebrow">COMPANY PROFILE</p><h2>{dealerCompany.name}</h2><dl className="dealer-data-list"><div><dt>Account status</dt><dd>{dealerCompany.status}</dd></div><div><dt>Market</dt><dd>{dealerCompany.region}</dd></div><div><dt>Account contact</dt><dd>{dealerCompany.contact}</dd></div></dl></article><article className="dealer-workspace-panel"><p className="eyebrow">SUPPORT</p><h2>Need a hand?</h2><p className="dealer-panel-copy">For allocation, terms, billing, or account access, contact your dealer representative.</p><a className="button button-dark" href="mailto:dealer-support@wemove.example">Email dealer support</a></article><article className="dealer-workspace-panel"><p className="eyebrow">TEAM</p><h2>Your people</h2><div className="dealer-people-list">{dealerTeam.map((member) => <div key={member.email}><strong>{member.name}</strong><span>{member.role}</span><a href={`mailto:${member.email}`}>{member.email}</a></div>)}</div></article><article className="dealer-workspace-panel"><p className="eyebrow">ADDRESSES</p><h2>Where orders go</h2><div className="dealer-address-list">{dealerAddresses.map((address) => <div key={address.label}><strong>{address.label}</strong>{address.lines.map((line) => <span key={line}>{line}</span>)}</div>)}</div></article></section></DealerWorkspace>;
}
